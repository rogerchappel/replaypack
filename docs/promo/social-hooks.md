# Social Hooks

## Short Posts

1. README examples drift when nobody keeps receipts. `replaypack` records the command, fixture hashes, output chunks, exit status, and matchers into a diffable JSONL pack.

2. `replaypack` is for small proof-oriented CLI smokes: record once, verify later, render Markdown for release notes or agent handoffs.

3. Not terminal cinema. A transcript pack you can diff, verify, and cite next to the example it supports.

## Demo CTA

```sh
npm run build
bash examples/release-note-demo.sh
```

