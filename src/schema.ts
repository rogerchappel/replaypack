export const replaypackSchemaSummary = {
  version: 1,
  schema: 'https://replaypack.dev/schemas/replaypack-v1.json',
  events: ['header', 'stream', 'result'],
  extension: '.replaypack.jsonl'
} as const;
